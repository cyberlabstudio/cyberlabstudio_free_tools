# 📦 Pack 10 Scripts Linux — Fiche Globale

## Présentation

Ce pack regroupe **10 scripts Linux gratuits**, conçus pour simplifier les tâches courantes du système.

Chaque script est :

* minimaliste

* lisible

* portable

* compatible avec la majorité des distributions Linux

* accompagné d’une fiche technique détaillée

Ce pack est fourni **gratuitement**, sans garantie, sans support et sans maintenance continue.

## Contenu du pack

Le pack contient :

* les **10 scripts** prêts à l’emploi

* les **10 fiches techniques GitHub** (`docs/scripts/`)

* les **10 fiches Notion** (`notion/`)

* la documentation générale (`installation`, `usage`, `changelog`)

## Liste des scripts

| Script | Fonction |
|--------|----------|
| `backup.sh` | Créer une sauvegarde compressée d’un dossier ou fichier |
| `clean.sh` | Nettoyer les fichiers temporaires et caches inutiles |
| `compress.sh` | Compresser un fichier ou dossier en `.tar.gz` |
| `diskcheck.sh` | Vérifier l’espace disque et analyser les dossiers volumineux |
| `netcheck.sh` | Tester la connectivité réseau et la résolution DNS |
| `search.sh` | Rechercher un mot-clé dans des fichiers ou dossiers |
| `sync.sh` | Synchroniser deux dossiers avec `rsync` |
| `sysinfo.sh` | Afficher les informations système essentielles |
| `update.sh` | Mettre à jour le système via APT |
| `useradd.sh` | Créer un utilisateur Linux avec options avancées |

Chaque script possède sa fiche technique dédiée dans :

```bash
docs/scripts/
```

## Installation

Voir la documentation complète :

```bash
docs/installation.md
```

## Utilisation

Les exemples d’utilisation et options sont disponibles dans :

```Code
docs/usage.md
docs/scripts/<nom_du_script>.md
```

## Licence

Ce pack est distribué sous licence :

```Code
LICENSE
```

## Support et maintenance

Ce pack est fourni **gratuitement**, sans :

* support utilisateur

* garantie

* maintenance continue

* correction systématique des bugs

Des contributions peuvent être proposées, mais **aucune prise en charge n’est garantie**.

## Version

La version actuelle est indiquée dans :

```Code
VERSION
```
